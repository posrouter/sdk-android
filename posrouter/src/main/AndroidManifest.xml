<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />
    <!-- Granted via Settings → Display over other apps; exempts background Activity starts (API 29+). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!--
      Merged into host apps (GoMenu, etc.). Without these, Android 11+ hides the kiosk from
      getPackageInfo / resolveActivity, so local_posrouter_kiosk looks "not installed"
      even when it is (demo works only because it declared the same queries itself).
    -->
    <queries>
        <package android:name="com.posrouter.kiosk" />
        <package android:name="ezypay.com.globe.cardpos" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="posrouter-kiosk" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="ezypos" />
        </intent>
    </queries>

    <application>
        <service
            android:name=".terminal.LensingTerminalService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".terminal.TerminalBootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
